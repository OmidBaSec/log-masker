# Direct dependencies, pinned. Requires Python 3.11+ (see README).
# requirements.lock holds the full resolved set — install that for a
# reproducible build:  pip install -r requirements.lock
fastapi==0.141.1
uvicorn==0.52.4
requests==2.34.2
keyring==25.7.0
msal==1.38.0
openpyxl==3.1.5
python-multipart==0.0.32
cryptography==50.0.0
